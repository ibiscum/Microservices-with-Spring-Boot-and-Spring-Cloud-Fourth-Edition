# Microservices with Spring Boot and Spring Cloud - Chapter 6

    docker run -t --rm -v ${PWD}:/app zavoloklom/dclint ./docker-compose.yml
    ./gradlew :microservices:product-service:test --tests PersistenceTests
